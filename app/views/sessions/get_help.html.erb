<html>
<head>
<script type="text/javascript">
<!--
function setHelpTime(){
	window["helpTime"] = new Date();
}

function updateTimer()
{
  var curTime = new Date();

  var curHours = curTime.getHours();
  var curMinutes = curTime.getMinutes();
  var curSeconds = curTime.getSeconds();

  var helpHours = window["helpTime"].getHours();
  var helpMinutes = window["helpTime"].getMinutes();
  var helpSeconds = window["helpTime"].getSeconds();

  var displayHours = curHours - helpHours;
  var displayMinutes = curMinutes - helpMinutes;
  var displaySeconds = curSeconds - helpSeconds;

  // Pad the minutes and seconds with leading zeros, if required
  displayMinutes = ( displayMinutes < 10 ? "0" : "" ) + displayMinutes;
  displaySeconds = ( displaySeconds < 10 ? "0" : "" ) + displaySeconds;

  // Convert the hours component to 12-hour format if needed
  // displayHours = ( displayHours > 12 ) ? displayHours - 12 : displayHours;

  // Convert an hours component of "0" to "12"
  // displayHours = ( displayHours == 0 ) ? 12 : displayHours;

  // Compose the string for display
  var displayString = displayHours + ":" + displayMinutes + ":" + displaySeconds;

  // Update the time display
  document.getElementById("clock").firstChild.nodeValue = displayString;
}

// -->
</script>
</head>
<!--body onload="updateClock(); setInterval('updateClock()', 1000 )"><-->
<body onload="setHelpTime(); setInterval('updateTimer()', 1000 )">
<div class="center">
<p class="blue_white"><%= "You are on the list to be helped." %></p>
<p class="blue_black_b">Your position in the list is </p>
<p class="lightTan_black"><%= @request.id%> </p>
<p class="blue_black_b"><span id="clock">&nbsp;</span></p>
</div>
</body>
</html>